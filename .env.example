# =============================================================================
# Let's Play API - Environment Configuration
# =============================================================================
# Copy this file to .env and update the values
# IMPORTANT: Never commit .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# MongoDB Configuration
# -----------------------------------------------------------------------------
# For Docker Compose: Use service name 'mongodb'
# For local development: Use 'localhost'
SPRING_DATA_MONGODB_URI=mongodb://mongodb:27017/letsplay
SPRING_DATA_MONGODB_DATABASE=letsplay

# MongoDB credentials (if authentication enabled)
# MONGODB_USERNAME=admin
# MONGODB_PASSWORD=password

# -----------------------------------------------------------------------------
# JWT Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Generate a strong secret with: openssl rand -base64 32
# IMPORTANT: Use a unique secret for each environment (dev, staging, prod)
JWT_SECRET=your-secret-here

# JWT token expiration (in milliseconds)
# Default: 900000 (15 minutes)
JWT_EXPIRATION=900000

# Refresh token expiration (in milliseconds)
# Default: 604800000 (7 days)
# JWT_REFRESH_EXPIRATION=604800000

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins
# Development default includes common frontend ports
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4200,https://localhost

# Production example:
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# -----------------------------------------------------------------------------
# Rate Limiting Configuration
# -----------------------------------------------------------------------------
# Number of requests allowed per time window
RATELIMIT_CAPACITY=5

# Time window in minutes
RATELIMIT_REFILL_MINUTES=1

# -----------------------------------------------------------------------------
# Spring Boot Profile
# -----------------------------------------------------------------------------
# Options: dev, test, prod
# Use 'dev' for local development with hot reload
# Use 'prod' for production deployment
SPRING_PROFILES_ACTIVE=dev

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Server port (default: 8080)
# Only needed if running locally without Docker
# SERVER_PORT=8080

# Logging level
# Options: TRACE, DEBUG, INFO, WARN, ERROR
# LOGGING_LEVEL_ROOT=INFO
# LOGGING_LEVEL_COM_LETSPLAY=DEBUG

# -----------------------------------------------------------------------------
# Traefik/SSL Configuration (Production)
# -----------------------------------------------------------------------------
# Your domain name for the API - used by Traefik for routing and SSL certificates
# IMPORTANT: This must match the domain configured in your Traefik instance
# Example: DOMAIN_API=api.yourdomain.com
DOMAIN_API=yourdomain.com

# -----------------------------------------------------------------------------
# Docker Compose Overrides
# -----------------------------------------------------------------------------
# Uncomment to override Docker Compose settings

# MongoDB exposed port (default: 27017)
# MONGODB_PORT=27017
